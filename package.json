{
  "name": "rev30",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @rev30/server --filter @rev30/client --parallel dev",
    "dev:client": "pnpm --filter @rev30/client dev",
    "dev:server": "pnpm --filter @rev30/server dev",
    "build": "pnpm -r build",
    "check": "pnpm format:check && pnpm lint:check && pnpm typecheck && pnpm test && pnpm build",
    "check:deprecated": "node scripts/check-deprecated-apis.mjs",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "oxlint --fix .",
    "lint:check": "oxlint .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check ."
  },
  "devDependencies": {
    "@types/node": "^24.13.1",
    "oxfmt": "^0.53.0",
    "oxlint": "^1.68.0",
    "oxlint-tsgolint": "^0.23.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "packageManager": "pnpm@11.5.2"
}
